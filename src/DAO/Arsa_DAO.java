package DAO;

import java.io.IOException;

public class Arsa_DAO extends Abstract_DAO{
    @Override
    public void Save(Object o) throws IOException {
        super.Save(o);
    }

    @Override
    public String[] getData(String filename) throws IOException {
        return super.getData(filename);
    }
}
